{
  "version": 2,
  "buildCommand": "cd cms && npm install && npm run build",
  "outputDirectory": "cms/dist",
  "builds": [
    {
      "src": "cms/dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "cms/dist/server.js"
    }
  ]
}
